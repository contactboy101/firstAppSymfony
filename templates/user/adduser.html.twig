{% extends 'base.html.twig' %}

{% block title %}Fiche utilisateur{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Création utilisateur</h1>
    <div class="content">    
        {{ form_start(form) }}
            {#{form_widget(form)}#}
            <div class="row">
                <div class="col-md-6">
                    {{ form_row(form.login) }}
                </div>
                <div class="col-md-6">
                    {{ form_row(form.password) }}
                </div>
                <div class="col-md-12">
                    {{ form_row(form.description) }}
                </div>    
            </div>
            <button type="submit" class="btn btn-secondary" id='save'>Enregistrer</button>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
